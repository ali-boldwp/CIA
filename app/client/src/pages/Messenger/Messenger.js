import React from 'react';
import MessengerPage from "./components/Header";

const Messenger = () => {
    return (
        <div>

            <MessengerPage />
        </div>
    );
};

export default Messenger;