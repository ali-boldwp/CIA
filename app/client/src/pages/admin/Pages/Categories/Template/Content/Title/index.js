import styles from "./style.module.css";

const Title = () => {

    return (
        <div className={ styles.wrapper }>
            <h1>
                Report title
            </h1>
        </div>
    );

}

export default Title;